/**
 * Signaling server entry (Phase 1 placeholder).
 *
 * Phase 2 implements: device registration, rooms, routing of
 * connection-request / offer / answer / ICE via Socket.IO.
 * The server NEVER receives screen video — only signaling messages.
 */
console.log('Signaling server arrives in Phase 2. See README architecture section.');
export {};
