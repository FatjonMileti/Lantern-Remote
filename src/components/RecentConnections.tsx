/** Recent connections placeholder (history store lands in Phase 10). */
export function RecentConnections() {
  return (
    <section className="card" aria-label="Recent connections">
      <h2>Recent Connections</h2>
      <p className="hint">No recent connections yet. History is stored locally from Phase 10.</p>
    </section>
  );
}
